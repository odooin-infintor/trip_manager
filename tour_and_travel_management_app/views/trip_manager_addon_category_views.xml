<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- FORM VIEW -->
    <record id="trip_manager_addon_category_view_form" model="ir.ui.view">
        <field name="name">trip.manager.addon.category.view.form</field>
        <field name="model">trip.manager.addon.category</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="is_guide_category" string="Is Guide Category"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- LIST VIEW -->
    <record id="trip_manager_addon_category_view_tree" model="ir.ui.view">
        <field name="name">trip.manager.addon.category.view.tree</field>
        <field name="model">trip.manager.addon.category</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- ACTION -->
    <record id="action_trip_manager_addon_category" model="ir.actions.act_window"> 
        <field name="name">Addon Categories</field>
        <field name="res_model">trip.manager.addon.category</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>